# API Contracts — Mini Binance

| Artifact | Description |
|----------|-------------|
| [`openapi.yaml`](./openapi.yaml) | REST v1 contract (target for `tradeup-api` and `tradeup-app`). |

Initial version aligned with `spec.md` and clarifications. Optionally generate a TypeScript client with OpenAPI Generator in a later phase.
